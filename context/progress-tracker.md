# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

- Brownfield adoption — initial implementation complete, context files being established.

## Current Goal

- Maintain and extend the existing CLI (add features, fix issues as they surface).

## Completed

- `internal/config` — GlobalConfig + LocalConfig TOML read/write
- `internal/ssh` — `~/.ssh/config` parser, SSH+SFTP connection, `UploadFile`, `ListDirs`
- `internal/git` — `TrackedFiles()`, `UntrackedFiles()` via `git ls-files`
- `internal/tui/hostpicker` — filterable bubbles/list of SSH hosts
- `internal/tui/dirpicker` — remote directory browser via SFTP
- `internal/tui/filepicker` — multi-select file picker (tracked pre-selected, untracked toggleable)
- `cmd/init` — 3-step flow: huh form → host picker → dir browser → save config
- `cmd/sync` — file picker TUI → SFTP upload with per-file ✓/✗ log
- `cmd/profiles` — list profiles with local default marked
- `cmd/root` + `main.go` — cobra wiring, `-v` flag, binary entry point
- **Unit 03 — sync flag defaults**: `LocalConfig.SyncUntracked`, `teleport config get|set|unset` subcommand, and post-sync warning when untracked files are skipped (`context/specs/03-sync-flag-defaults.md`)
- **Unit 04 — beam (commit-driven sync)**: `teleport beam` subcommand, commit picker + beam file picker TUIs, `git.CommitsAhead/FilesInCommits/FileAtCommit`, `ssh.UploadBytes/Remove` (`context/specs/04-beam-commits.md`)
- **Unit 05 — status**: `teleport status [profile]` with `--pending`/`-p`, `ssh.Client.RemoteSHA256`, full and pending-mode classification (`==`, `!=`, `??`, `--`), exit code 1 on drift (`context/specs/05-status.md`)
- **Unit 06 — file icons**: extended `fileTypeIcon` in `internal/tui/syncprogress.go` with Nerd Font glyphs for ~60 extensions plus basename matches for `Dockerfile`/`Makefile`/`.gitignore` (`context/specs/06-file-icons.md`)
- **Unit 07 — clean**: `teleport clean [profile]` discards dirty changes on the remote git work tree (`git checkout -- . && git clean -fd`), with `--ignored`/`-x` to also wipe gitignored files; `beam --clean`/`-c` chains it before the beam (`beam -cs` = clean → beam → sync); new `ssh.Client.RunCommand` + `ssh.ShellQuote` helpers; new `tui.RunCleanConfirm` (`context/specs/07-clean.md`)
- **Unit 08 — config polish**: extracted reusable `helpDoc`/`renderHelp` in `cmd/help.go` and applied styled help (badge title, sections, Nerd Font icons, Examples, new `Config keys` table) to `teleport config` and `get|set|unset`; `teleport -h` output stays byte-identical; `teleport config get` (no args) now shows the resolved profile (host:path), explicit `<unset>` / `(from global)` / `(not found in global config)` states, and a `last sync` block with absolute timestamp + Spanish humanized "hace …"; `LocalConfig.LastSync time.Time` + `config.TouchLastSync()` persisted on successful `sync`, `beam`, and `clean`; `config get <key>` unchanged for scripts (`context/specs/08-config-polish.md`)
- **Unit 09 — SSH password auth**: `ErrNoAuthMethods` sentinel in `internal/ssh/client.go`; `ConnectWithPassword(host, pw)`; `connectToHost` in `cmd/clean.go` catches the sentinel and calls `promptPassword` (masked `huh` input); `connectToProfile` delegates to `connectToHost`; `init`, `sync`, `status` migrated to use the shared helpers — all commands now fall back to password prompt when no key/agent is available (`context/specs/09-ssh-password-auth.md`)
- **Unit 10 — Remove profile**: `teleport profiles remove <name>` (alias `rm`) elimina un profile del global config; `GlobalConfig.RemoveProfile` helper en `internal/config/config.go`; warning si el profile eliminado era el `default-profile` del local config actual, sin modificar el local config (`context/specs/10-remove-profile.md`)
- **Unit 12 — Pull**: `teleport pull [profile]` downloads files changed on the remote to local working tree; pre-checks ensure local working tree is clean and both remotes are at the same commit; uses `git status --porcelain=v1 -z` to detect modified/added/deleted/untracked files; deletes via `os.Remove`, downloads via SFTP with per-file ✓/-/✗ output; `git.HasUncommittedChanges()` + `git.LocalHEAD()` helpers; `ssh.Client.DownloadFile()` method (`context/specs/12-pull.md`)
- **Unit 13 — Beam branch picker**: `teleport beam --branch/-b <branch>` flag to specify source branch; new branch picker TUI when flag omitted and multiple branches exist (current pre-selected); new `git.LocalBranches()` + `git.CommitsAheadOf(branch)` helpers with upstream/remote-only logic; new `internal/tui/branchpicker.go` bubbletea model (`context/specs/13-beam-branch-picker.md`)
- **Unit 14 — Ship**: `teleport ship <bin>` deploys a local executable to a remote `bin/` dir. New `internal/bindetect` package sniffs ELF/Mach-O/PE magic bytes to pick the target OS; `GlobalConfig.BinProfiles` map (keyed by `linux|macos|windows`, validated on load) holds per-OS `{host, bin_path}`; `ssh.Client.RemoteWritable` probes the destination and `ship` falls back to `sudo -n` then masked-password `sudo -S` via new `ssh.Client.RunCommandStdin` when needed; `--os`/`--to`/`--name` flags override detection, path and remote basename; `teleport init` gains a huh multi-select that lets the user configure sync + any subset of bin profiles in one session (reusing the host picker per target and a header-aware `tui.RunDirPickerWith`); `teleport profiles` now prints `Sync profiles` and `Bin profiles` sections (`context/specs/14-ship.md`)
- **Unit 15 — Beam sent-tracking**: `teleport beam` recuerda qué commits ya se enviaron a cada profile y lo refleja en el commit picker. Nuevo `LocalConfig.BeamedCommits map[string]map[string]time.Time` (profile → SHA → cuándo) con helpers `SentSet`/`MarkBeamed`/`PruneBeamed`; el picker pre-selecciona solo los no-enviados, badge verde `iconSent` `󰗠` + asunto dim en los enviados, y tecla `u` re-selecciona los pendientes; granularidad por-archivo (un commit se marca solo si todos sus archivos del beam subieron OK); `RunSyncProgress` ahora devuelve las rutas fallidas (no un contador) para atribuir éxito por commit; auto-poda de SHAs que ya no están ahead (`context/specs/15-beam-sent-tracking.md`)
- **Unit 17 — Beam manual sent-mark**: el commit picker de `teleport beam` gana marcado manual de "ya enviado" sin re-desplegar — `m` togglea la marca del commit bajo el cursor (en vivo), `M` togglea todos (simétrico). Corrige el sent-tracking cuando la identidad por SHA exacto falla (rama nueva sin historial, rebase/amend/cherry-pick, primer beam desde otra máquina). Las marcas se acumulan en la sesión del picker y se persisten al beamed store por-profile **solo al confirmar (enter)**, en cuanto el picker confirma (sobreviven aunque se cancele el file picker o el upload); `ctrl+c` las descarta. Nuevo `LocalConfig.ApplyBeamedDelta` aplica el delta (added+removed) en una sola escritura atómica; las remociones se acotan a los commits realmente mostrados (no toca SHAs históricos fuera de la ventana). `beam --auto` se salta el picker → marcado inerte por diseño (`context/specs/17-beam-manual-sent-mark.md`)
- **Unit 18 — Beam file/diff viewer**: en el beam file picker, `v` abre un visor del archivo completo (blob del commit) y `d` el diff que ese commit introdujo, ambos estilo `bat`/`delta` con resaltado de sintaxis, números de línea y `+/−` coloreados; `tab` alterna archivo⇄diff, `esc`/`q` vuelve al picker con el estado intacto. Nuevo paquete `internal/highlight` (chroma, Catppuccin-Mocha, Go puro sin CGO); nuevo sub-modelo `internal/tui/beamfileviewer.go` con `bubbles/v2/viewport`; helpers `git.FileDiffAtCommit`/`git.FileBeforeCommit`; el I/O (git+highlight) vive en un closure `beamViewerLoader` en `cmd/beam.go` (invariante #3) con carga perezosa + caché por modo (`context/specs/18-beam-file-diff-viewer.md`)
- **Unit 16 — Shell**: `teleport shell [profile]` abre un shell interactivo en el remoto ya parado en el `path` del profile. Zero-config: deriva `host`+`path` del profile de sync existente (reusa `resolveProfile` y `ssh.ShellQuote`) y reemplaza su propio proceso con el `ssh` del sistema vía `syscall.Exec` → comportamiento y performance idénticos a un `ssh` a mano (TTY/colores/agente/`~/.ssh/config` nativos) y sin proceso teleport colgado durante la sesión. Comando efectivo: `ssh -t <host> "cd '<path>' && exec zsh"` (shell remoto fijo `zsh`). Código aislado en `cmd/shell.go` + split por plataforma `cmd/shell_unix.go` (`syscall.Exec`) / `cmd/shell_windows.go` (`exec.Command` + `os.Exit`); `sync`/`beam`/`ship`/`clean`/`pull` intactos (`context/specs/16-shell-command.md`)

## In Progress

- None.

## Next Up

- (sin unidad planificada)

## Unit 14 Summary

- **Unit 14 — Ship**: new top-level `teleport ship <bin>` subcommand for deploying built CLI binaries to a remote `bin/` directory in three automated steps: SFTP upload → `chmod +x` → `mv` into place (with sudo escalation when the SSH user can't write the destination). Target OS is detected from magic bytes (ELF→linux, Mach-O incl. fat→macos, MZ/PE→windows) by the new `internal/bindetect` package and selects one of up to three bin profiles stored in `~/.config/teleport/config.toml` under `[bin_profiles.<os>]`. Bin profiles live alongside sync profiles in `GlobalConfig.BinProfiles` (`map[string]BinProfile`, validated on load — unknown OS keys raise a decode error). `internal/ssh/client.go` adds `RemoteWritable` (POSIX `[ -w dir ]` probe) and `RunCommandStdin` (used to feed the sudo password via stdin so it never appears in the remote process list). Overwrite is silent (`mv -f`). When sudo prompts and the user cancels, the binary is left under `/tmp/teleport-ship-<pid>-<ts>/<name>` and the message points there. `teleport init` was rewritten around a `huh.MultiSelect` that lets the user pick any combination of `Sync profile` and `Bin profile — {Linux, macOS, Windows}`; the sync flow is preserved when chosen and per-OS bin flows reuse the host picker plus a header-parameterised `tui.RunDirPickerWith` (defaults: `/usr/local/bin` for linux/macos, `/` for windows). `teleport profiles` now prints two labeled sections, omitting either when empty. Help table and examples in `cmd/help.go` updated; no new third-party deps.

## Unit 13 Summary

- **Unit 13 — Beam branch picker**: `teleport beam` now accepts `--branch/-b` flag to specify the source branch for commits. If not provided and multiple branches exist, a new TUI branch picker opens (pre-selecting current branch). If only one branch exists, the picker is skipped. New `git.LocalBranches()` helper returns current branch first, followed by others; new `git.CommitsAheadOf(branch)` uses explicit upstream check or `--not --remotes` fallback. New `internal/tui/branchpicker.go` implements single-select bubbletea model with keys `↑/k`, `↓/j`, `enter`, `ctrl+c`. `cmd/beam.go` adds `resolveBranch` helper and refactored commit sourcing. Message now shows `"Nothing to beam — no local commits on <branch> ahead of remote."` when sync'd. No new dependencies.

## Open Questions

- Should `teleport sync` preserve file permissions on the remote, or always use default SFTP permissions?
- ~~Should the dir browser show hidden directories (dotfiles) as an opt-in?~~ Resolved: show all dirs unconditionally — needed to navigate to paths like `~/.local/bin`.
- Is `InsecureIgnoreHostKey` fallback acceptable for first-connection UX, or should we prompt to add the key?

## Architecture Decisions

- **bubbletea v2 over v1**: v2 changes `Init()` to return `tea.Cmd` (not `(Model, Cmd)`) and `View()` to return `tea.View` (not string). All models must use `tea.NewView(s)`.
- **No CGO**: binary must cross-compile; all dependencies are pure Go.
- **BurntSushi/toml for config**: chosen over `encoding/json` for human-readable, comment-friendly config files that users may edit manually.
- **`InsecureIgnoreHostKey` fallback**: used when `~/.ssh/known_hosts` is missing or unreadable, to avoid blocking the init flow on first use. Acceptable for a local developer tool; not acceptable for production services.

## Session Notes

- 2026-06-30 — Navegación entre archivos dentro del visor de beam (mejora de Unit 18, sin spec). En el visor file/diff (`beamFileViewer`) ahora `←`/`→` (alias `n`/`p`) saltan al archivo anterior/siguiente sin salir del pager, con wrap en los extremos. La lógica vive en el picker (`BeamFilePicker.stepFile(delta)` en `internal/tui/beamfilepicker.go`): mueve el cursor dentro del **rango del filtro activo** (`activeRange`, módulo n) y reabre el visor vía `openViewer(m.viewer.mode)` — así el modo file⇄diff se conserva al paginar (lees el diff de cada archivo en secuencia). No-op si el rango tiene un solo archivo. `updateViewing` mapea `right`/`n`→`stepFile(1)`, `left`/`p`→`stepFile(-1)` (interceptados antes del `default` que delega scroll al viewport). El visor (`beamFileViewer`) gana campos `idx`/`total` (posición 1-based dentro del rango activo) que `newBeamFileViewer` recibe y `openViewer` calcula (`m.cursor-lo+1`, `hi-lo`); el footer muestra `←/→=file N/M` solo cuando `total>1`. Tests nuevos en `internal/tui/beamfilepicker_test.go` (`TestBeamFilePickerStepFile`: avance+wrap con tracking de posición, wrap hacia atrás, modo preservado al paginar, no-op en rango de un archivo; helper `pickerWith` abre el visor vía `Update(keyPress(...))`). README actualizado (sección beam, atajos del visor) y CHANGELOG `[Unreleased] Added`. Sin dependencias nuevas. `go build`/`go vet`/`go test ./...` limpios. Va en commit `[ADD]` (código+test+changelog) + `[DOC]` (README) aparte.
- 2026-06-30 — Fix (visor de beam, conteo de commits engañoso, sin spec). El file picker mostraba `len(legend)` (commits que poseen un archivo ganador tras la deduplicación de `FilesInCommits`) como "N commits", así que seleccionar 5 commits que reescriben los mismos archivos leía "3 commits" sin explicación (aunque los 5 sí se beamean y se acreditan vía `commitPaths`). Fix en `internal/tui/beamfilepicker.go`: nuevo campo `selectedN` (= `len(commits)` pasados, total seleccionado) seteado en `NewBeamFilePicker`; la línea de conteo (modo "all") ahora muestra `N commits · M with files` cuando `selectedN > len(legend)`, y `N commits` a secas cuando coinciden. El filtro `←/→` sigue recorriendo solo los commits contribuyentes. Test nuevo `TestBeamFilePickerCommitCount` (caso con gap y caso sin gap) + helper `stripANSI`. Commiteado en `e6607b4` `[FIX] beamfilepicker`. CHANGELOG `[Unreleased] Fixed`.
- 2026-06-29 — Regla nueva + limpieza de idioma: **todo el texto compilado en el binario va en inglés, sin excepción** (regla dura en `context/code-standards.md` → sección "Language"; anula el idioma de trabajo español de la sesión y obliga a convertir a inglés cualquier cadena en español que se toque). Conversión de las cadenas en español que quedaban: `cmd/config.go` `humanizeSince` (de "hace N minutos/horas/días" + meses `ene..dic` → "N minutes/hours/days ago" + `time.Format("Jan 2")`); `internal/tui/beamfilepicker.go` status line (`filtrar por commit` → `filter by commit`); footers y estados del visor de la Unit 18 (`ver archivo/ver diff` → `view/diff`, `cargando…` → `loading…`, `「sin contenido」` → `「empty」`, `N líneas` → `N lines`, nota `(antes de borrar)` → `(before delete)`). `CHANGELOG.md` y los resúmenes en chat siguen en español; solo cambian las strings del binario. Sin tests dependientes de las cadenas viejas.
- 2026-06-29 — Unit 18 (pulido del diff): cabecera de archivo + barra de hunk. Se **oculta** el bloque de cabecera de archivo del diff (`diff --git`/`index`/`---`/`+++`) — redundante con el header del visor (que ya muestra path + SHA); en `highlight.Diff` el caso pre-hunk hace `continue`. Cada hunk ahora abre con una **barra a ancho completo** (`hunkBar`, bg `238` rellenada hasta el ancho de render) que separa el rango `@@ -a,b +c,d @@` en gris (`245`) del contexto de sección (función/clase que git nombra) en accent (`117`), con una línea en blanco de separación entre hunks. Para el ancho completo se hiló `width` por la firma: `tui.FileContentFunc(fc, mode, width)` (el picker pasa `m.width` vía `loadCmd`, capturado al abrir el visor), `beamViewerLoader(fc, mode, width)` y `highlight.Diff(raw, filename, width, profile)`; el resize no re-flowea la barra (aceptable). Se quitaron los estilos `hunkStyle`/`metaStyle` ya sin uso. Test de `Diff` ajustado a la nueva firma. Verificado visualmente: headers fuera, barra full-width con rango dim + contexto accent, hunks separados. (Propuesta elegida por el usuario sobre maqueta: ocultar header + rango `@@`+contexto.)
- 2026-06-29 — Unit 18 (mejora): el diff del visor pasó a **delta-style**. `highlight.Diff` ahora recibe el `filename` y resalta la sintaxis del código *dentro* de cada línea (helper `lineHighlighter` por-línea, reusa el lexer de `pickLexer`), con gutter de dos columnas (old/new) tintado por tipo de cambio: añadido chip verde (fg `83`/bg `22`), borrado chip rojo (fg `210`/bg `52`), contexto dim `241`; cabecera de hunk en barra tenue, cabeceras de archivo pre-hunk dim. Los números de línea salen de parsear `@@ -a,b +c,d @@` (`hunkRe`). Se factorizaron `pickLexer` y `lineHighlighter` (compartidos por `Code` y `Diff`); se quitaron los estilos por-línea simples del v1. Call site `cmd/beam.go` pasa `fc.Path`; test de diff ajustado a la nueva firma (filename plaintext para no romper aserciones por escapes ANSI). Verificado visualmente con un diff de muestra (gutters tintados + keywords/comentarios coloreados, adds/dels correctos).
- 2026-06-29 — Unit 18 implementada: beam file/diff viewer. Nuevo paquete `internal/highlight` que envuelve `github.com/alecthomas/chroma/v2` (Go puro, sin CGO → invariante #5): `Code(src, filename, profile)` (lexer por `lexers.Match`→`Analyse`→`Fallback`, formatter por `colorprofile` TrueColor→TTY16m/ANSI256→TTY256/ANSI→TTY16/else→NoOp, estilo `catppuccin-mocha` que ya trae chroma, gutter de números de línea derecha en `dimStyle` 241, tope `lineCap=5000` líneas sin resaltar), `Diff(raw, profile)` (coloreo por línea: `+` verde 82, `−` rojo 203, `@@` barra dim, contexto dim 241; cuenta adds/dels saltando cabeceras `+++`/`---`) e `IsBinary` (NUL en primeros 8 KB). Nuevos helpers en `internal/git/git.go`: `FileDiffAtCommit` (`git show --format= <sha> -- <path>`) y `FileBeforeCommit` (`git show <sha>^:<path>`, para ver un archivo que el commit borró). Nuevo sub-modelo `internal/tui/beamfileviewer.go` (`beamFileViewer`) sobre `charm.land/bubbles/v2/viewport` (primer uso de viewport en el repo): cabecera bat-style (icono de tipo + path + `· lang · 󰜘 short · N líneas`, o `iconDiff 󰦓 · diff · short · +adds −dels` con checkStyle/deleteStyle), placeholder `「binary file · N bytes」`/`「sin contenido」`, caché por modo (`cache`/`loaded`) + estado `loading`/`err`; teclas `g/G` propias, el resto (j/k, ↑/↓, ctrl+d/u, f/b) al keymap por defecto del viewport. Integración en `internal/tui/beamfilepicker.go`: nuevos campos `load FileContentFunc`, `viewing`, `viewer` y `shortBySHA`; `v`/`d` abren el visor sobre el archivo del cursor (orthogonal al `tab` de selección), `updateViewing` maneja esc/q (cierra, estado del picker intacto), `tab` (alterna modo, carga perezosa del otro modo si no está en caché) y delega scroll; `loadCmd` emite un `tea.Cmd` que llama al loader y devuelve `viewerContentMsg`. `RunBeamFilePicker`/`NewBeamFilePicker` ganan el parámetro `load`. El I/O vive en el closure `beamViewerLoader` de `cmd/beam.go` (detecta `colorprofile.Detect(os.Stdout, os.Environ())`, usa `FileAtCommit`/`FileBeforeCommit`/`FileDiffAtCommit` + `highlight.*`) — el TUI no hace I/O (invariante #3). Tipos `ViewerMode`/`ViewerContent`/`FileContentFunc` y `viewerContentMsg` definidos en `beamfileviewer.go`. Tests nuevos: `internal/git/git_test.go` (repo throwaway con `t.Chdir`: add/modify/delete; cubre FileAtCommit/FileBeforeCommit/FileDiffAtCommit) y `internal/highlight/highlight_test.go` (lang conocido=Go con líneas, fallback para extensión desconocida, conteo de diff sin contar cabeceras, IsBinary). Dependencia nueva: `github.com/alecthomas/chroma/v2`. `go build`/`go vet`/`go test ./...` limpios, gofmt OK (los `hostpicker.go`/`ship.go` que marca gofmt son preexistentes, ajenos). La verificación interactiva del visor (scroll, tab archivo⇄diff, resaltado real) requiere correr el binario contra un repo con commits-ahead, queda al usuario. Spec en `context/specs/18-beam-file-diff-viewer.md`.
- 2026-06-29 — Unit 17 implementada: beam manual sent-mark (adaptación de un prompt genérico que el usuario ya implementó en otro CLI con flujo similar; era el pendiente opcional "toggle manual `m`" anotado en Unit 15). `internal/tui/commitpicker.go`: nuevo `origSent` (snapshot del set sent al construir) + nuevo struct exportado `SentMarkDelta{Added,Removed}`; teclas `m` (toggle del cursor, mutando el `sent` vivo → badge/dim en vivo) y `M` (toggle masivo simétrico sobre todos los commits); método `SentDelta()` que diffea `sent` vs `origSent` **iterando solo `m.commits`** (así un SHA del store fuera de la ventana de commits-ahead nunca se reporta como removido); `RunCommitPicker` ahora devuelve `([]git.Commit, SentMarkDelta, error)`. `internal/config/config.go`: nuevo `ApplyBeamedDelta(profile, add, remove, t)` que aplica add (vía `MarkBeamed`) + remove (delete tolerante a ausentes) en una pasada y borra el profile si queda vacío; `MarkBeamed` se conserva para la ruta automática post-upload. `cmd/beam.go`: captura el delta del picker y persiste con `ApplyBeamedDelta`+`SaveLocal` **justo al confirmar el picker** (antes del file picker/conexión/upload y antes del early-return de "no commits selected", para que las marcas sobrevivan a cualquier cancelación posterior); fallo de save = warn, no fatal. `beam --auto` intacto (sin picker → marcado inerte). Footer del picker actualizado: `... u=unsent  m=mark-sent  M=all-sent  enter=confirm ...`. Tests nuevos: `internal/tui/commitpicker_test.go` (toggle individual, vuelta-a-original sin delta, masivo simétrico, y el caso "nunca reporta SHA fuera de los commits mostrados") con helper `keyPress` (`tea.KeyPressMsg{Text}`); y `TestApplyBeamedDelta` en `internal/config/config_test.go` (add, mixto, remove de ausente=no-op, vaciado borra profile, scope por profile, delta vacío). Sin dependencias nuevas. `go build`/`go vet`/`go test ./...` limpios, gofmt OK. Verificación interactiva del TUI (subir/bajar badge en vivo, persistencia tras cancelar) requiere correr el binario contra un repo con commits-ahead, queda al usuario. Spec en `context/specs/17-beam-manual-sent-mark.md`.
- Initial implementation produced 2026-05-06.
- All 14 files committed in a single `[ADD] teleport` root commit (hash `1fbf8f2`).
- Context files scaffolded via `/spec-driven-dev init --from-code` in the same session.
- 2026-05-12 — Unit 03 implemented: warning emitted **after** the sync TUI exits (rendering full-height inline view would otherwise scroll the warning off-screen if printed before).
- 2026-05-15 — Unit 04 implemented: `teleport beam`. Per-file content fetched with `git show <sha>:<path>` from the most-recent selected commit that touched the file; deletes via `SFTP.Remove` (idempotent on missing). Reused existing `SyncProgress` TUI for the upload phase.
- 2026-05-15 — Added `--then-sync`/`-s` flag to `beam`: chains a working-tree sync on the same SSH connection after the beam phase succeeds. Order is fixed (beam → sync) so disk content always wins over the beamed blob.
- 2026-05-15 — Unit 05 implemented: `teleport status`. SHA256 streamed over SFTP per file; no remote state persisted. `--pending` reuses `git.CommitsAhead`/`FilesInCommits` and flags `'D'` paths still present on the remote with `--`.
- 2026-05-15 — Unit 06 implemented: expanded `fileTypeIcon` map. Codepoints chosen via the `nerd-fonts` skill (seti-*, dev-*, md-*, cod-*, custom-elixir). Basename switch handles `Dockerfile`, `Makefile`, `.gitignore`/`.gitattributes`/`.gitmodules` before the extension lookup. Fallback unchanged (cod-file).
- 2026-05-15 — Unit 07 implemented: `teleport clean`. Pure SSH (no SFTP) — runs `git rev-parse --is-inside-work-tree` as a safe guard, parses `git status --porcelain=v1 -z` (with `--ignored` when `-x` is set), shows a bubbletea confirm grouped by revert/remove/restore/ignored, then runs `git checkout -- . && git clean -fd [-x]`. `beam -c` reuses the helper and the open connection; refactored `beam` profile resolution and connect into shared `resolveProfile` / `connectToProfile` helpers in `cmd/clean.go`.
- 2026-05-25 — Unit 08 implemented: `config` polish. Refactor in `cmd/help.go` extracts `helpDoc`/`renderHelp`; verified byte-identical `teleport -h` against pre-refactor binary via temp clone + diff. `config`, `config get`, `set`, `unset` each register `SetHelpFunc` with their own `helpDoc` (own title, tagline, examples) and share a `Config keys` table. `config get` (no args) now prints a styled overview: explicit `<unset>` / `(from global)` for `default-profile`, resolved `host:path`, and `last sync` (absolute `YYYY-MM-DD HH:MM:SS -ZZ` + Spanish "hace N minutos/horas/días/MMM D"). `config get <key>` left untouched for `$()` use. `LocalConfig.LastSync time.Time` (omitempty) persisted by `config.TouchLastSync()` — called at the end of `sync`, `beam`, and `clean` (incl. the already-clean path); failures are warn-logged, not fatal.
- 2026-05-27 — Unit 12 implemented: `teleport pull [profile]`. Adds `git.HasUncommittedChanges()` and `git.LocalHEAD()` helpers; adds `ssh.Client.DownloadFile()` for SFTP downloads with local parent dir creation and partial-write cleanup; new `cmd/pull.go` subcommand orchestrates pre-checks (local working tree clean, remote at same commit), fetches remote dirty files via `git status --porcelain=v1 -z`, and downloads/deletes with styled per-file output. Added pull to help output in `cmd/help.go`. No new dependencies.
- 2026-05-28 — Unit 14 implemented: `teleport ship`. Pure orchestration in `cmd/ship.go` (Stat → bindetect → resolve `BinProfile[os]` → `connectToHost` → `UploadFile` to `/tmp/teleport-ship-<pid>-<ts>/<name>` → `chmod +x` → `RemoteWritable` probe → `mv -f` plain or via `sudo -n`/`sudo -S` with masked `huh` password). `internal/bindetect/bindetect.go` is a single-purpose magic-byte sniffer (8 bytes read, table match, returns `ErrUnknownFormat` otherwise). `config.LoadGlobal` validates every `bin_profiles.<key>` against `bindetect.Valid`. `internal/tui/dirpicker.go` was refactored to thread a header through `NewDirPickerWith` / `RunDirPickerWith`; the old constructors keep their previous string ("  Remote Directory Browser") for back-compat. `cmd/init.go` was rebuilt around `huh.NewMultiSelect` with at-least-one validator; the sync sub-flow now uses `RunDirPickerWith(client, "/", "  Select sync directory")` and the bin sub-flow uses per-OS start paths plus `"  Select bin/ directory for <os>"`. `cmd/profiles.go` prints separate `Sync profiles` and `Bin profiles` sections. Smoke-tested: `teleport ship ./teleport` without config returns the expected `no bin profile configured for macos …` message; `teleport ship ./go.mod` rejects with `not a recognised executable binary`.
- 2026-06-02 — Fix (Unit 14 follow-up): `teleport ship` dejaba archivos de 0 bytes reportando éxito. Causa: `MaxPacketUnchecked(1<<22)` (4 MiB) excedía `SFTP_MAX_MSG_LENGTH` de OpenSSH (256 KB) → el server descartaba el payload; y `defer dst.Close()` ignoraba el error que el server solo reporta en el close. Fixes en `internal/ssh/client.go`: paquete a `MaxPacket(32768)`; `UploadFile`/`UploadFileProgress`/`UploadBytes` cierran el handle explícitamente y propagan el error de `Close()`; nuevo helper `verifyUpload` compara stat remoto vs tamaño local; `progressReader` expone `Size() int64` para que `sftp.File.ReadFrom` use la ruta concurrente (pipelined) en vez de la secuencial; y `ConnectWithPassword` pasa de `MaxPacketChecked(1<<20)` (rechazado por ser >32 KB, rompía el auth por contraseña) a `MaxPacket(32768)`. Nuevo invariante #6 en `architecture.md`. Verificado manualmente por el usuario (subida correcta y más rápida). No es unidad nueva: no requiere spec. Commit `0fd1e0f`.
- 2026-06-09 — Consistencia (tecla de toggle = `tab` en toda la app): regla unificada — seleccionar/deseleccionar es `tab` en todos los pickers multi-selección, con `space` como alias. `internal/tui/filepicker.go` aceptaba solo `space` → ahora `case "tab", " "` y textos de ayuda actualizados (`tab to toggle` / `tab=toggle`). `cmd/init.go` `multiSelectKeyMap`: `tab` agregado a `km.MultiSelect.Toggle` (con `space`/`x`), ya estaba fuera de `Next`. `commitpicker`/`beamfilepicker` ya cumplían. Los navegadores `dirpicker`/`localfilepicker` se dejan con `tab`=descender (single-select, no togglean). No es unidad nueva.
- 2026-06-09 — Mejora (beam send view, short SHA): la vista de subida ahora muestra el hash corto del commit junto al icono, en formato `[a1b2c3]` y con el color del commit. El marcador por ruta pasó de `lipgloss.Style` a una struct `tui.BeamMarker{Style, Short}`; `RunSyncProgressMarked` y `SyncProgress.markers` usan el nuevo tipo. `cmd/beam.go` arma `shortBySHA` desde `selectedCommits` y construye `markers[path]=BeamMarker{styles[sha], shortBySHA[sha]}` (se quitó el import de lipgloss, ya sin uso). No es unidad nueva.
- 2026-06-09 — Mejora (beam send view, cubo de color por commit): la vista de progreso del beam ahora antepone a cada archivo el cubo `iconCube` `󰆧` con el color de su commit, igual que el file picker. Asignación de color extraída a `beamCommitColors` (fuente única) + nueva exportada `tui.BeamFileStyles(changes, commits)` que devuelve SHA→`lipgloss.Style`; `SyncProgress` gana `markers map[string]lipgloss.Style` (ruta→estilo) y se renderiza el cubo en cada línea ✓/✗; nueva `RunSyncProgressMarked` (la `RunSyncProgress` simple delega en un `runSyncProgress` interno con `markers=nil`). `cmd/beam.go` calcula `styles := tui.BeamFileStyles(allChanges, selectedCommits)` (mismo set que el picker, para que los colores coincidan exactamente) y construye `markers[path]=styles[sha]` solo para los uploads. No es unidad nueva. Sin dependencias nuevas.
- 2026-06-09 — Fix (Unit 15, atribución de commits enviados): el marcado de "enviado" se hacía por SHA del archivo, pero `FilesInCommits` deduplica y solo conserva la versión del commit más nuevo cuando varios commits tocan la misma ruta; así, los commits cuyos archivos eran pisados por otros más nuevos nunca se marcaban (aparecían como no-enviados aunque su contenido sí viajó). Fix: marcar por **cobertura de rutas**, no por SHA. `FilesInCommits` ahora también devuelve `commitPaths map[string][]string` (rutas que tocó cada commit, renames divididos), en la misma pasada de git; `cmd/status.go` ignora el nuevo retorno con `_`. `rememberBeamedCommits` arma `covered` (rutas del beam sin fallo) y nuevo helper puro `commitsFullySent(commitPaths, covered)` marca un commit solo si TODAS sus rutas están cubiertas — así un commit con archivo superado por otro más nuevo sí se acredita. Se auto-corrige al siguiente beam (los no-marcados quedan pre-seleccionados). Test nuevo `cmd/beam_test.go` cubre el caso de archivo compartido. No es unidad nueva.
- 2026-06-09 — Unit 15 implementada: beam sent-tracking. `LocalConfig` gana `BeamedCommits map[string]map[string]time.Time` (TOML `[beamed_commits.<profile>]`, SHA = timestamp) + helpers puros `SentSet`/`PruneBeamed`/`MarkBeamed` (mutadores; el caller hace `SaveLocal`). `RunCommitPicker(commits, sent)` / `NewCommitPicker(commits, sent)`: pre-selecciona `!sent[sha]`, badge verde `iconSent` (`sentStyle` color 82) + asunto `dimStyle` en enviados (columna en blanco alineada en los demás), tecla `u` = re-seleccionar no-enviados, footer actualizado. `RunSyncProgress` cambia firma de `(int, error)` → `([]string failedPaths, error)`; actualizados los 3 call sites (`cmd/sync.go`, beam upload, `runChainedSync`). `cmd/beam.go`: captura `profileName` de `resolveProfile`, poda `BeamedCommits[profile]` a los commits-ahead antes del picker, y `rememberBeamedCommits` marca como enviado cada commit con ≥1 archivo en la selección y sin ningún fallo, persistiendo **antes** de devolver el error de fallo parcial (los commits OK se guardan aunque otro falle). `TouchLastSync` recarga de disco así que no pisa el guardado de BeamedCommits. Tests nuevos: `internal/config/config_test.go` (helpers beamed) y verificación visual del picker. Spec en `context/specs/15-beam-sent-tracking.md`. Pendientes opcionales fuera de alcance: `--reset-sent` y toggle manual `m`.
- 2026-06-09 — Mejora (beam file picker, filtro por commit + paleta 16): el picker gana un modo filtro por commit con ←/→ para que sea usable con muchos commits (antes, con 33 commits, la leyenda empujaba la lista a 1 fila visible). Estado nuevo en `BeamFilePicker`: `filter int` (-1 = todos), `ranges []beamRange` (rango contiguo de archivos por commit, precomputado tras el sort por commit), `width int`. En modo "todos" la cabecera es una línea de conteo + hint (`N commits ◂ ←/→ filtrar ▸`) y se listan todos los archivos (ventana deslizante); en modo filtrado la cabecera colapsa a una línea (cubo coloreado + short SHA + asunto truncado + `i/N`) y solo se muestran los archivos de ese commit. `cycleFilter` cicla con wrap a través del pseudo-entry "todos" y reposiciona el cursor; `up/down` se acotan al rango activo; `a` togglea solo el filtro activo; las selecciones persisten entre filtros (`SelectedChanges` es global). Paleta ampliada de 8 → 16 colores (`beamCommitPalette`, documentada en `ui-context.md`). Nuevo helper compartido `truncate` en `listwindow.go`. No es unidad nueva. Sin dependencias nuevas.
- 2026-06-09 — Mejora (beam file picker, coloreo por commit): el picker "Files from selected commits" ahora agrupa los archivos por el commit del que provienen y los colorea para que se lea de un vistazo. `RunBeamFilePicker`/`NewBeamFilePicker` reciben ahora `[]git.Commit` (los commits seleccionados) además de los `[]git.FileChange`; se aprovecha que `FileChange.SHA` ya guarda el commit ganador de cada archivo. Nueva `beamCommitPalette` (8 colores ANSI documentados en `ui-context.md`) asignada en orden de commit a cada commit que aporta al menos un archivo. La vista muestra una leyenda (una línea por commit: cubo coloreado `iconCube` `󰆧` + short SHA + asunto) y luego la lista agrupada por commit (orden de commits, alfabético dentro de cada grupo) con la ruta tintada del color del commit; los borrados conservan su sufijo rojo `(delete)`. `cmd/beam.go` pasa `selectedCommits` al picker. No es unidad nueva. Sin dependencias nuevas.
- 2026-06-09 — Fix (TUI list overflow): los pickers basados en cursor renderizaban **todos** los ítems de golpe, así que listas largas (p.ej. muchos commits en `beam`) desbordaban la altura de la terminal y rompían la UI visualmente. Nuevo helper compartido `internal/tui/listwindow.go` (`computeWindow` + `scrollUpHint`/`scrollDownHint`) que calcula una ventana deslizante que mantiene el cursor visible y reserva 2 filas para indicadores `↑ N more` / `↓ N more`. Aplicado a `commitpicker`, `beamfilepicker`, `branchpicker`, `localfilepicker`, `dirpicker` y `filepicker` (este último reparte el alto entre las secciones tracked/untracked vía `sectionRows`, priorizando la lista interactiva). Cada modelo gana un campo `height` (default 24) actualizado vía `tea.WindowSizeMsg`. Test unitario `listwindow_test.go` cubre los casos límite de `computeWindow`. No es unidad nueva: fix visual a features existentes, sin spec. Sin dependencias nuevas.
- 2026-06-17 — Unit 16 implementada: `teleport shell`. Comando aislado en `cmd/shell.go` que resuelve el profile con el `resolveProfile` existente (`cmd/clean.go`), arma el comando remoto `cd <ShellQuote(path)> && exec zsh` (constante `shellRemoteShell="zsh"`; degrada a `exec zsh` si el path está vacío) y hace `exec.LookPath("ssh")` + `execSSH(sshBin, ["ssh","-t",host,remoteCmd])`. `execSSH` está separado por build tags: `cmd/shell_unix.go` (`//go:build !windows`) usa `syscall.Exec` (reemplaza el proceso, no deja teleport corriendo, performance = ssh directo); `cmd/shell_windows.go` (`//go:build windows`) usa `exec.Command` con stdio heredado + `os.Exit(code)` como fallback de compilación. Registro vía `init()` propio (patrón de `ship`), sin shorthand en root. Fila + ejemplo agregados a `cmd/help.go` (icono `iconGear`). NO usa el stack SSH puro de Go de `internal/ssh` (ese sigue siendo el transporte de sync/beam/ship/clean/pull). Verificado: `go build`+`go vet` limpios en darwin, gofmt OK, comando visible en `teleport shell --help` y en el help general. El cross-compile a Windows ya estaba roto desde antes por `internal/tui/shipprogress.go` (usa `syscall.SYS_IOCTL`/`TIOCGWINSZ` sin build tags) — ajeno a este cambio; el `shell_windows.go` queda listo para cuando se arregle. La verificación interactiva (pwd/$0/sin proceso colgado) requiere server real, queda al usuario. Spec en `context/specs/16-shell-command.md`.
- 2026-05-28 — Unit 13 enhancement: branch picker gains always-on filter input (`textinput.Model` focused on open, type to filter, `esc` clears, `↑↓`/`ctrl+p`/`ctrl+n` navigate filtered list, `enter` selects highlighted match) and Nerd Font icons (`󰘬 ` md-source_branch for branches, `󰋜 ` md-home for `main`/`master`). Spec doc added at `context/specs/13-beam-branch-picker.md`.
